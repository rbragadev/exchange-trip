# API
API_PORT=3000
API_HOST=0.0.0.0
NODE_ENV=development

# Banco (API rodando localmente)
# Troque localhost por db se a API rodar dentro do Docker no mesmo compose
DATABASE_URL=postgresql://app:app@localhost:5432/app_local
